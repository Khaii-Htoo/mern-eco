# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @kha/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t kha-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system kha-auth-service && \
          adduser --system -G kha-auth-service kha-auth-service

COPY apps/auth-service/dist kha-auth-service/
COPY apps/auth-service/package.json kha-auth-service/
RUN chown -R kha-auth-service:kha-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix kha-auth-service --omit=dev -f install

CMD [ "node", "kha-auth-service" ]
